# Deploying Tracer

The application runs on **Render** (API + static frontend, from the
`render.yaml` blueprint) and the database lives on **Neon**.

Two providers rather than one, for a specific reason: Render's free PostgreSQL
is deleted after 30 days, which would break the link without warning. Neon's
free tier persists, so the deployment keeps working.

**Roughly 30 minutes of setup, plus 10–20 minutes to load the data.**

---

## Why PostgreSQL and not MySQL

The project runs on MySQL locally, which is what Chapter Three documents.
Render does not offer managed MySQL, so the hosted copy uses PostgreSQL.
Nothing in the application changed: SQLAlchemy generates the SQL for whichever
dialect the connection string names, so the models, queries and routes are
identical. The only code that needed dialect awareness was the bulk loader,
which used a MySQL-specific statement to suspend foreign-key checks.

If asked, this is the honest one-liner:

> The system is database-agnostic through SQLAlchemy. It runs on MySQL locally,
> as described in the report, and on PostgreSQL in the hosted demonstration,
> because that is what the hosting provider offers. No application code differs.

---

## Step 1 — Create the database on Neon

1. Sign up at **neon.tech** (GitHub login works).
2. **Create project** — name it `tracer`, pick the region closest to the Render
   region you will use.
3. On the dashboard, open **Connection string** and choose the **Pooled
   connection** (the host contains `-pooler`). Copy it.

It looks like:

```
postgresql://user:PASSWORD@ep-xxxx-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

Keep this to hand — it is used twice, in steps 2 and 3.

## Step 2 — Deploy the application on Render

1. Go to **dashboard.render.com** → **New** → **Blueprint**.
2. Connect the repository `HabeebAdewole/fraud-detection-system`.
3. Render reads `render.yaml` and shows two services: `tracer-api` and
   `tracer-web`.
4. It prompts for **`DATABASE_URL`** — paste the Neon string from step 1.
5. **Apply**.

`SECRET_KEY` and `JWT_SECRET_KEY` are generated by Render; there is nothing
else to fill in. The first build takes 5–10 minutes, mostly scikit-learn.

## Step 3 — Load the database

The Neon database starts empty. The Elliptic CSVs are 657 MB and are not in the
repository, so the load runs **from your machine** against Neon directly.

```bash
# from the repository root
set DATABASE_URL=<the same Neon connection string>

backend\venv\Scripts\python.exe ml_pipeline\elliptic_load_db.py
cd backend && venv\Scripts\python.exe seed_users.py
```

The loader detects a remote database and sends larger batches. Expect 10–20
minutes for 203,769 transactions and 234,355 edges over a home connection. It
is idempotent — safe to re-run if the connection drops.

## Step 4 — Check it

Render appends a random suffix to service hostnames, so the API is not at the
plain service name. Copy the URL from the `tracer-api` service page — for the
current deployment it is `https://tracer-api-68u0.onrender.com`.

```bash
curl https://tracer-api-68u0.onrender.com/api/health
```

```json
{ "status": "ok", "environment": "production", "database": "ok" }
```

Then open **https://tracer-web.onrender.com** and sign in with the seeded
accounts.

---

## Two things to know about the free tier

**The API sleeps after 15 minutes of inactivity.** The first request afterwards
takes about 50 seconds while the instance wakes and reloads the 162 MB of model
artifacts. Every request after that is fast. This is fine for a portfolio link;
it is not fine for a live demonstration in front of an audience — open the page
a minute beforehand to wake it.

**Render's free PostgreSQL is deleted after 30 days**, which is why the
database is on Neon and `render.yaml` has no `databases:` block. Neon's free
tier persists, so the link keeps working. Supabase is an equivalent
alternative; both are PostgreSQL and both work with the URL normaliser already
in `config.py`.

---

## Demo credentials and DEMO_MODE

The seeded accounts (`admin` / `admin123`, `analyst` / `analyst123`) are
published in this repository so anyone can look around the live demo. That
means every visitor arrives holding the administrator password, and the admin
panel can change passwords and delete accounts — so without a guard a stranger
could lock you out of your own deployment with a single request.

`render.yaml` therefore sets **`DEMO_MODE=true`** on the hosted API. It makes
the write half of the admin panel return 403:

| Route | DEMO_MODE=true |
|---|---|
| `GET /api/admin/users` | allowed |
| `GET /api/admin/metrics`, `GET /api/admin/curves` | allowed |
| `POST /api/admin/users` | 403 |
| `PATCH /api/admin/users/<id>` | 403 |
| `DELETE /api/admin/users/<id>` | 403 |
| `POST /api/admin/metrics` | 403 |

Everything worth demonstrating still renders; nothing a visitor does can
outlast their session. The flag is reported by `GET /api/health` and
`GET /api/auth/me`, and the frontend reads the latter to disable the
account-management controls rather than offering buttons that only fail.

It defaults to off, so local development and any private deployment keep the
full admin panel. For a real deployment, leave `DEMO_MODE` unset and change the
seeded passwords instead — edit `backend/seed_users.py` before running it, or
change them through the admin panel afterwards.

Independently of the flag, the admin panel now refuses to delete your own
account, and refuses to delete or demote the last remaining administrator;
either would leave the deployment with no way in short of re-seeding.

---

## Environment variables

| Variable | Set by | Purpose |
|---|---|---|
| `ENV` | blueprint | `production` enables the strict posture |
| `SECRET_KEY` | Render | Flask session signing; boot fails without it |
| `JWT_SECRET_KEY` | Render | Token signing; boot fails without it |
| `DATABASE_URL` | Render | Normalised automatically; TLS added for remote hosts |
| `CORS_ORIGINS` | blueprint | Restricts the API to the frontend origin |
| `DEMO_MODE` | blueprint | Makes admin writes read-only; off unless set |
| `VITE_API_URL` | blueprint | Baked into the frontend at build time |

Changing `CORS_ORIGINS` requires a redeploy of the API; changing
`VITE_API_URL` requires a rebuild of the frontend, since Vite inlines it.
